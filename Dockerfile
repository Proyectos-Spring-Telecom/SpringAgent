# Placeholder Fase 4: imagen de producción para SpringAgent (PaddleOCR + FastAPI).
# Construir cuando se definan capas base, modelo OCR y variables de entorno.

FROM python:3.11-slim
WORKDIR /app
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001"]
